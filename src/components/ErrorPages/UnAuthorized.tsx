import {withRouter} from "react-router-dom";

const UnAuthorized = () => {
    return (
        <div>
            <h1>UnAuthorized</h1>
        </div>
    );
};

export default withRouter(UnAuthorized);
