import React from 'react';

const Logout = (props) => {
    return (
        <div>
            <h2>Welcome Julian</h2>
            <button>logout</button>
        </div>
    );
}

export default Logout;
